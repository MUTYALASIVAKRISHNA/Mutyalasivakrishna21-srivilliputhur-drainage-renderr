services:
  - type: web
    name: srivilliputhur-drainage-risk
    env: python
    buildCommand: pip install -r requirements.txt && python database.py
    startCommand: gunicorn app:app
    envVars:
      - key: PYTHON_VERSION
        value: 3.12.0
